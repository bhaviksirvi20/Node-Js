# 🚀 Server Learning

This project is created by **Bhavik Sirvi** to practice both **Node.js (HTTP module)** and **Express.js** servers.

---

## 📁 Project Structure

---

## 🧠 What You’ll Learn
- How to create a server using the native `http` module  
- How to build a server using **Express.js**
- The difference between `res.write()` and `res.send()`
- Routing basics (`/`, `/about`, `/contact`, etc.)
- How to run a Node.js server using `npm start`

---

## ⚙️ How to Run

### ▶️ For Normal Server
1. Open terminal inside `normal-server`
2. Run:
   ```bash
   node index.js
